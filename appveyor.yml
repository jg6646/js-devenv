environment:
  nodejs_version: '10'

  matrix:
    install:
      - ps: Install-Product node $env:nodejs_version $env:platform
      - npm install

  test_script:
    - node --version
    - npm --version
    - npm test

  build: off 
